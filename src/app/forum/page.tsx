import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Форум | Noliner shop",
};

const Forum: React.FC = () => {
  return <div>Форум</div>;
};

export default Forum;
